# ============================================
# FINANCE MODULE - PRODUCTION REQUIREMENTS
# GoDaddy cPanel Compatible (Shared Hosting)
# ============================================
# These libraries work on shared cPanel hosting without system dependencies

# ============================================
# 1. MONEY & CURRENCY ✅ (All Safe)
# ============================================
django-money==3.4.1
py-moneyed==3.0
forex-python==1.8           # Calls external APIs, no system deps
Babel==2.13.1
# decimal is Python stdlib - no install needed

# ============================================
# 2. ACCOUNTING & COMPLIANCE ✅ (All Safe)
# ============================================
django-iban==0.3.1
python-stdnum==1.19         # GOLD for PAN/GSTIN/VAT validation
# beancount - Optional, install separately if needed

# ============================================
# 3. PDF PROCESSING ✅ (cPanel-Safe Only)
# ============================================
# ✅ These work without system dependencies
pdfplumber==0.11.0          # Best for unstructured PDFs
PyPDF2==3.0.1               # Basic PDF text extraction
Pillow>=10.1.0              # Image processing
dateparser==1.2.0           # Flexible date parsing

# ⛔ EXCLUDED - Need system packages (Java, Ghostscript, Tesseract)
# tabula-py - Requires Java JRE
# camelot-py[cv] - Requires Ghostscript + OpenCV
# pytesseract - Requires Tesseract OCR binary

# ============================================
# 4. NLP & RULES ✅ (Mostly Safe)
# ============================================
nltk==3.8.1                 # Download corpora locally, ship with project
rapidfuzz==3.5.2            # Fast fuzzy string matching (has wheels)
business-rules==1.0.1       # Rule engine
Cerberus==1.3.5             # Data validation
regex==2023.10.3            # Advanced regex

# ⚠️ spaCy - See dev requirements (heavy wheels, optional for rule-based NLP)

# ============================================
# 5. ANALYTICS ✅ (Core libraries only)
# ============================================
pandas>=2.1.4               # Essential for data analysis
numpy>=1.26.2               # Pandas dependency
plotly==5.18.0              # Pure Python/JS, perfect for web dashboards

# ⚠️ statsmodels, matplotlib, seaborn - See dev requirements

# ============================================
# 6. SECURITY ✅ (All Safe)
# ============================================
django-axes==6.1.1          # Login attempt protection
django-defender==0.9.7      # Rate limiting (works with Django cache, no Redis required)
django-encrypted-model-fields==0.6.5  # PII encryption
cryptography==41.0.7        # Encryption primitives
# python-keyring - Not needed on cPanel (use env vars)

# ============================================
# 7. UTILITIES ✅ (All Safe)
# ============================================
arrow==1.3.0                # Better datetime handling
python-json-logger==2.0.7   # Structured logging
PyYAML==6.0.1               # YAML config support

# ============================================
# 8. DJANGO EXTENSIONS ✅ (All Safe)
# ============================================
django-fsm==2.8.1           # Finite state machines
django-auditlog==2.3.0      # Automatic audit trails
django-import-export==3.3.1 # CSV/Excel import/export
django-simple-history==3.4.0 # Model history tracking
django-concurrency==2.5     # Optimistic locking

# ============================================
# 9. CACHING (Django Cache Backend Only)
# ============================================
# Use Django's built-in cache backends (database, file, dummy)
# ⛔ EXCLUDED for shared cPanel:
# redis - Not available on shared hosting
# django-redis - Requires Redis server
# celery - Can't run background workers

# For cPanel, use:
# - Database cache: python manage.py createcachetable
# - File-based cache: Built-in, no dependencies
# - Memcached: If GoDaddy provides it (check with host)

# ============================================
# TOTAL: 25 Production-Safe Libraries
# ============================================
# All pure Python or have prebuilt wheels for common platforms
# No system dependencies (Java, Tesseract, Redis, etc.)
# Tested and stable on shared hosting environments
